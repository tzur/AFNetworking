{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "description": "Event marking that the run count of the application on the device had been updated",
  "properties": {
    "run_count": {
      "description": "current run count of the application on the device",
      "type": "number"
    }
  },
  "required": ["run_count"]
}
